## PM cycle — archive overview-navigation-fix spec

`tk ready -a ralph` empty → PM role. Spec lifecycle sweep found
`docs/specs/2026-04-08-overview-navigation-fix/` complete (epic
`soc-nrtu` + all 3 children closed). Stamped `archived: 2026-04-08`
in `_overview.md` and `git mv`'d the directory under
`docs/specs/archive/`.

No other PM action: `.msgs/` empty, no other live specs, no stale
in-progress tickets. Backlog for ralph is empty after this archive.
